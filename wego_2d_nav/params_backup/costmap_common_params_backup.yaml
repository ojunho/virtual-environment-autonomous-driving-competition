footprint: [[-0.1, -0.1], [-0.1, 0.1], [0.3, 0.1], [0.3, -0.1]]
footprint_padding: 0.0 # 0.02

transform_tolerance: 0.2
map_type: costmap

inflation_layer:
  enabled: true
  cost_scaling_factor: 1.0
  inflation_radius: 0.07 # 0.1  모든 벽, 장애물에 대한 확장 값 -> 이상하게 얘만 영향을 줌
  inflation_unknown: true

static_layer:
  enabled: true
  map_topic: "/map"
